# to create this environment run:
# conda env create -f create_new_env.yaml

name: bilby

channels:
  - conda-forge
  - anaconda

dependencies:
  - python = 3.11
  - bilby
  - pip
  - python-ldas-tools-framecpp
  - python-nds2-client

  - pip:
    - bilby_pipe

